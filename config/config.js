/**
 *
 * Config constants
 *
 */

export const Config = {
  REALM_PATH: 'reactNativeQueue.realm', // Name of realm database.
  REALM_SCHEMA_VERSION: 2 // Must be incremented if data model updates.
};